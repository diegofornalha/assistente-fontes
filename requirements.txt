# Web framework (suporta Pydantic v2)
fastapi>=0.100.0,<0.101.0
# Necessário para WebSocket funcionar (instala websockets/wsproto etc.)
uvicorn[standard]>=0.20.0

# Templates, forms, env-vars e auth
jinja2>=3.1.2
python-multipart>=0.0.6
python-dotenv>=1.0.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.2
python-jose[cryptography]>=3.3.0

# Cliente Anthropic (Claude)
anthropic>=0.18.0

# LlamaIndex + extras para embeddings HuggingFace (sentence-transformers)
llama-index>=0.12.43
llama-index-embeddings-huggingface>=0.2.0

# FAISS vector store
faiss-cpu>=1.7.3

# Renderização de markdown no chat
markdown2>=2.4.10,<3.0.0
